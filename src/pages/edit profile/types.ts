import React from "react";
import {Value} from "../../components/inputs/types";
export type formData = {
    username: string;
    fullName: string;
    birthday: Value;
    email: string;
    phoneNumber: string;
    gender: string
}